# PHP API Project

This is a PHP API project that downloads files, processes CSV data into a database, and provides an interface.

## Setup
1. Run `composer install` to install dependencies.
2. Configure your database and endpoints in the `config/` directory.

## Execution
Run the index script to execute the data pipeline.
